apiVersion: v1
kind: Service
metadata:
  name: finbot-svc
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: finbot
  ports:
    - protocol: TCP
      port: 80        # 외부 접속 포트 (브라우저용)
      targetPort: 8080 # 내부 연결 포트 (Flask용)
