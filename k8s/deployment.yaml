apiVersion: apps/v1
kind: Deployment
metadata:
  name: finbot-deploy
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: finbot
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: finbot
        app.kubernetes.io/version: v5.0  # 👈 여기는 v5.0으로 수정 (Grafana용)
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: finbot
        image: 192.168.1.18/demo/finbot:v15 # ⭐⭐ v4 이미지 태그 사용 ⭐⭐
        ports:
        - containerPort: 8080
        # 오토스케일링(HPA)을 위해 CPU 제한 설정 필수
        resources:
          requests:
            cpu: "100m"
          limits:
            cpu: "200m"
